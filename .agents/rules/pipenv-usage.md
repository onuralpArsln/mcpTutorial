---
trigger: always_on
---

This project uses pipenv
for isntall use pipenv  as in exmaple: 
"python3 -m pipenv install mcp google-genai pydantic python-dotenv"

to run also pipenv as in example:
"python3 -m pipenv run python3 client.py"